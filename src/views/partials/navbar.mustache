<nav class="navbar navbar-expand-lg navbar-dark bg-dark bg-opacity-75 fixed-top">
  <div class="container-fluid px-4">
    <a class="navbar-brand" href="/">
      ProjWeb<span class="text-danger fw-bold">Movies</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <form class="d-flex ms-auto me-3" role="search" action="/search">
        <input class="form-control form-control-sm me-2 bg-dark text-white border-secondary"
               type="search" name="q" placeholder="Procurar filmes">
        <button class="btn btn-sm btn-outline-danger" type="submit">Pesquisar</button>
      </form>

      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link {{#isHome}}active{{/isHome}}" href="/">Início</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{#isWatchlist}}active{{/isWatchlist}}" href="/watchlist">Watchlist</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{#isFavorites}}active{{/isFavorites}}" href="/favorites">Favoritos</a>
        </li>
        {{#isAuthenticated}}
          <li class="nav-item">
            <a class="nav-link" href="/profile">{{username}}</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Sair</a>
          </li>
        {{/isAuthenticated}}
        {{^isAuthenticated}}
          <li class="nav-item">
            <a class="nav-link {{#isLogin}}active{{/isLogin}}" href="/login">Entrar</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {{#isRegister}}active{{/isRegister}}" href="/register">Registar</a>
          </li>
        {{/isAuthenticated}}
      </ul>
    </div>
  </div>
</nav>
